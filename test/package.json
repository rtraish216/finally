{
  "name": "finally-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright E2E and API tests for FinAlly",
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test --project=api",
    "test:e2e": "playwright test --project=e2e",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
